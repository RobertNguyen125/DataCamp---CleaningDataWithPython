# glob() from glob libraries to find files based on pattern so we can combine a large
# amount of files without manually import each file with pandas
#
# glob():
#   - use wild cards:
#     '*': match a lot of characters
#     '?': only 1 character
